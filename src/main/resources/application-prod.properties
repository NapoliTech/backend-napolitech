# ==============================
# ? Banco de dados
# ==============================
spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://db:3306/pizzaria_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATASOURCE_USERNAME:napolitech}
spring.datasource.password=${DATASOURCE_PASSWORD:napolitech_dev}
spring.datasource.driver-class-name=${DATASOURCE_DRIVER:com.mysql.cj.jdbc.Driver}

# ==============================
# ?? Servidor
# ==============================
server.port=${SERVER_PORT:8080}

# ==============================
# ? JPA / Hibernate
# ==============================
spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQLDialect}
spring.jpa.properties.hibernate.format_sql=true

# ==============================
# ? Logging
# ==============================
logging.level.root=INFO
logging.level.org.hibernate.SQL=${LOGGING_SQL:DEBUG}
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=${LOGGING_BINDER:TRACE}
logging.level.org.springframework=INFO
logging.level.com.pizzaria.backendpizzaria=DEBUG

# ==============================
# ? Email (configurável via env)
# ==============================
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:mailsendernoreplyv8@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gycihyclbdscmhor}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}

# ==============================
# ? OAuth2 (opcional)
# ==============================
# spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:}
# spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:}
# spring.security.oauth2.client.registration.google.redirect-uri=${GOOGLE_REDIRECT_URI:http://localhost:8080/login/oauth2/code/google}

# ==============================
# ? RabbitMQ
# ==============================
broker.exchange.name=${BROKER_EXCHANGE_NAME:napolitech-exchange}
broker.queue.name=${BROKER_QUEUE_NAME:pedidos-queue}
broker.routing.key=${BROKER_ROUTING_KEY:pedidos-routing-key}

spring.rabbitmq.host=${RABBITMQ_HOST:rabbitmq}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_DEFAULT_USER:admin}
spring.rabbitmq.password=${RABBITMQ_DEFAULT_PASS:napolitech}
spring.rabbitmq.virtual-host=/
spring.rabbitmq.connection-timeout=60000
spring.rabbitmq.listener.simple.retry.enabled=true
spring.rabbitmq.listener.simple.retry.max-attempts=3
spring.rabbitmq.listener.simple.retry.initial-interval=2000

